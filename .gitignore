# The cosign private key a local signing test may leave in the checkout: the
# repository trusts cosign.pub alone and the key lives in the SIGNING_SECRET
# repository secret (docs/workflow.md § Repository settings).
cosign.key
