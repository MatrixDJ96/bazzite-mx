# The inner dockerd of a docker-in-docker devcontainer cannot load kernel
# modules and fails to start without this one on kernels newer than 6.11.7
# (devcontainers/features#1235, ublue-os/bluefin#2365).
iptable_nat
