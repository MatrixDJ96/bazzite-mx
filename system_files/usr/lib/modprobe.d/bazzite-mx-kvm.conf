# A guest reading a model-specific register KVM does not implement (Windows,
# some anti-cheat drivers) gets zeros instead of a #GP, and the kernel log
# stays quiet. Bazzite asks for the same behaviour with kernel arguments; kvm
# is a module in this kernel, so the option ships in the image instead.
options kvm ignore_msrs=1 report_ignored_msrs=0
